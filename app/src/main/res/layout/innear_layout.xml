<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data class="NewsBinding">
        <variable
            name="NewsModel"
            type="com.codinginflow.mvvmrecyclerviewcomparewithsimplerecyclerviewandroid.viewModel.NewsModel" />
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        android:orientation="vertical"
        android:padding="10dp">

        <TextView
            android:id="@+id/titleval"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="@color/colorWhite"
            android:textSize="20sp"
            android:text="@{NewsModel.Title}"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/descval"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:textColor="@color/colorWhite"
            android:text="@{NewsModel.Desc}"
            android:textSize="14sp" />
    </LinearLayout>
</layout>